# Checkpoints

Train locally:

```bash
bash scripts/train.sh
```

Default output: `models/lora_finetuned/` (LoRA adapter + tokenizer).

Legacy paths under `experiments/checkpoints/` may still exist from older runs but are no longer the default.
